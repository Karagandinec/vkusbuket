# Вкус Букет — карта экосистемы

> Индекс проекта. Проверено по кабинетам 30.08.2026.
> Последняя правка рекламных кабинетов — **22.08.2026**. Смотреть свежее окно
> (7–14 дней), а не июль: июль — это уже отработанные претенденты.
> **Читать до любых выводов про рекламу, аналитику и атрибуцию.**
> Рекламная петля уже построена и работает. Не заявлять, что «аналитики нет»
> или «UTM не настроены», не сверившись с этим файлом и с кабинетами.

## Что где живёт

Этот репозиторий — **только POS/бэк-офис** (React + Supabase): касса, склад,
производство, смены, предзаказы, отчёты. Сайтов и рекламного кода здесь нет и
быть не должно. Аналитику искать в кабинетах, а не в `src/`.

| Слой | Где | Подключено к сессии |
|---|---|---|
| POS / бэк-офис | этот репозиторий, Supabase | код — да, данные — нет |
| Сайт 1 | `vkusbuket.kz` (Tilda) | через GA4 |
| Сайт 2 | `klubnikavshokolade.kz` (Tilda) | через GA4 + Search Console |
| Instagram | `@vkusbuket.karaganda` (ID `17841410537730847`) | Windsor `instagram` |
| Реклама | Google Ads ×2, Meta ×5 | частично, см. ниже |

## Аккаунты

### GA4
| Ресурс | ID | Электронная торговля |
|---|---|---|
| vkusbuket.kz - Караганда | `397943910` | **нет** — 0 транзакций |
| klubnikavshokolade.kz | `548805252` | **есть и работает верно** |

### Google Ads — их два, и подключён не тот
| Кабинет | Customer ID | Что крутит | В Windsor |
|---|---|---|---|
| **vkusbuket.kz_new** | `8747974784` | **Performance Max — основной объём** | ❌ **НЕТ** |
| klubnikavshokolade.kz | `3977282678` (397-728-2678) | одна Search-кампания | ✅ да |

Кампании в `vkusbuket.kz_new`: `S-Performance` (PMax), `LPM - 11` (PMax),
`14.02.2026` (PMax), `Создание спроса (2024-11-12)` (Demand Gen).
За 01.07–29.08 дал **4 806 сессий и 884 конверсии** на vkusbuket.kz.

⚠️ Через Windsor виден только маленький кабинет klubnikavshokolade.kz
($70, 505 кликов). **Не выдавать его за весь Google Ads.** Расход по
`8747974784` без подключения кабинета не виден вообще.

### Meta Ads (Business `238339980390445` «Vkusbuket.karaganda»)
| Кабинет | ID | Статус | Запросы |
|---|---|---|---|
| Вкус Букет · AI Караганда | `3522529827982603` | ACTIVE — рабочий | ✅ |
| Виталий Бош | `465521244732733` | ACTIVE, расхода нет | ✅ |
| — | `1686952041405037` | **UNSETTLED** | ❌ |
| Казахстан Новый | `586810819730079` | **UNSETTLED** | ❌ |
| SMM_PRO | `2045584352547123` | CLOSED | ❌ |

Суффикс ID кампании = кабинет: `…0455` → AI Караганда, `…0715` → Виталий Бош.
В GA4 встречается кампания `120253721507010226` (суффикс `0226`) — значит
крутится ещё один кабинет, недоступный для запросов. 321 сессия за июль–август.

## Рекламная петля — как размечено (работает)

Разметка сквозная, метки живут в `session_source/medium/campaign/content` GA4
(в `landing_page` их не искать — GA4 их оттуда вырезает).

| Точка входа | source / medium | campaign | content |
|---|---|---|---|
| Meta, лента Instagram | `ig / paid` | ID кампании Meta | ID объявления |
| Meta, Facebook | `fb / paid` | ID кампании | ID объявления |
| Meta, Audience Network | `an / paid` | ID кампании | ID объявления |
| Meta, Threads | `th / paid` | ID кампании | ID объявления |
| Ссылка в шапке Instagram | `ig / social` | — | `link_in_bio` |
| Google Ads | `google / cpc` | имя кампании | автопометка |
| ChatGPT | `chatgpt.com / ai-assistant` | — | — |
| **2ГИС** | `link.2gis.ru` / `link.2gis.com` referral | — | — |

То есть: площадки Meta разделены по source, кампания и объявление проброшены
ID-шниками, ссылка в шапке помечена отдельно. **Это сделано намеренно.**

⚠️ GA4 относит `ig/fb/an/th` не в «Paid Social», а в «Paid Other» и
«Cross-network» — его дефолтный список источников их не знает. Это дефект
группировки GA4, **не** отсутствие меток. Смотреть source/medium, а не
Default channel group.

## Как устроена реклама Meta: чемпион-претендент

Кабинет работает по схеме «чемпион–претендент», а не «крутим всё подряд».
Это читается прямо в именах: `Ступень 1` / `Купившие` / `Горячие` — стадия
воронки; `чемпионы`, `чемпион-сборка` — победившая связка; `v2`, `ролики v2` —
итерация; `пост-1` vs `ролики 9:16` — тест креатива; `все плейсменты` vs
`stories+reels` vs `авто-плейсменты` — тест размещений; `муж 30–60` vs
`жен 25–65` vs `18–65` — тест аудиторий; `подарок-2` vs `чайная пара` — тест
оффера. География почти везде `Караганда 30км`.

**Последняя правка кабинета — 22.08.2026 17:58.** Сейчас активна ровно одна
кампания: `WA Переписки · 1С v2 · чемпион-сборка · 16.08`, бюджет $10/день,
цель OUTCOME_ENGAGEMENT. Остальное **выключено намеренно** — это результат
консолидации в чемпиона, а не простой.

Цена диалога по шагам: `Ступень 1` (июль) $1,99 → `1С v2` (весь период) $1,55
→ **последние 7 дней $1,24**. Схема работает и улучшается.

⚠️ **Не называть паузы «простоем».** Дни без показов = выключенные претенденты.
Прежде чем говорить о простое, смотреть `effective_status` и `updated_time`.

## Качество трафика по каналам (16–29.08)

| Источник | Сессий | Вовлечён. | Ср. длит. |
|---|---|---|---|
| `google / cpc` (vkusbuket) | 442 | 42 % | 53 с |
| `google / cpc` (klubnika) | 279 | 36 % | 58 с |
| `google / organic` | 45 | 51 % | 194 с |
| `ig / social` | 29 | **97 %** | 144 с |
| `l.instagram.com` | 2 | 100 % | **675 с** |
| `chatgpt.com` | 7 | 86 % | 376 с |

Платный трафик сидит на сайте втрое меньше и вовлекается вдвое хуже
органики и Instagram. **Отсутствие продаж по рекламным кликам — это свойство
трафика, а не поломка учёта.** Люди с рекламы смотрят и уходят; покупают те,
кто пришёл иначе.

## Что действительно не связано (и что — НЕ поломка)

1. **Продажи приходят серверно.** Транзакции `klubnikavshokolade.kz` летят с
   `(not set)` источником и без посадочной: за 16–29.08 — 5 заказов на
   64 040 ₸ при одной сессии. Это отправка с бэкенда по факту заказа, а не
   потеря данных. Учёт денег корректен, отсутствует только привязка к сессии.
2. **vkusbuket.kz** — электронной торговли в GA4 нет: 0 транзакций при ~10 тыс.
   сессий. Конверсии считаются событиями, без денег. Это факт, а не вывод.
3. **GA4 ↔ POS.** В карточке клиента (`POS.js:124`) поля источника нет:
   `id, name, phone, discount_percent, created_at`. Все `source` в коде — способ
   оплаты (`Expenses.js`) или локация склада (`Production.js`).
4. **Search Console** подключён только к `klubnikavshokolade.kz`.

⚠️ **Запрет на вывод «петля разорвана».** Ноль продаж по рекламному клику
означает ровно одно: пришедшие по рекламе не купили. Это не доказывает
поломку разметки. Разметка проверена и работает (см. таблицу выше). Прежде чем
говорить о разрыве — показать, что событие не доехало, а не что продажи нет.

## Сверка данных (валидация, 30.08.2026)

Транзакции GA4 `klubnikavshokolade.kz` сверены с `products.json`:
7 из 11 совпадают с прайсом **до тенге** (Набор 25 = 18 900, Набор 8 = 7 500,
Букет XS = 13 400, Набор 12 = 10 200, Букет S = 15 900, и пара 8 шт + XS =
20 900). Вывод: **электронная торговля настроена верно.**

Мусор: два тестовых заказа на `1 ₸` и `100 ₸` (самая дешёвая позиция прайса —
1 000 ₸). Реальная выручка за 06–29.08 — **111 340 ₸ за 9 заказов**, средний
чек **12 371 ₸** (не 10 131 ₸).

## Как правильно смотреть цифры

- **Meta:** MCP `mcp__Meta_ads__*`, кабинет `3522529827982603`. Только он и
  `465521244732733` отвечают на запросы.
- **Google Ads / GA4 / Search Console / Instagram:** Windsor.ai
  (`get_fields` → `get_data`). Supermetrics не авторизован ни к чему.
- **Каналы:** брать `session_source_medium`, а не `session_default_channel_group`.
- **Валюта:** расходы Meta и Google — в USD, выручка — в тенге. Курс не
  подставлять; сравнивать каналы по деньгам можно только после того, как
  появится сквозная связь с продажами.

## 2ГИС — статус сделки и как мерить (30.08.2026)

**Бланк заказа `БЗ_84-84-935800` от 27.08.2026, НЕ подписан.**
Пакет «Старт Медиа»: объявление в поисковой выдаче, рубрика «Сувенирные
композиции». 188 268 ₸ за 6 мес (31 378 ₸/мес), выход 01.09.2026–28.02.2027,
два платежа по 94 134 ₸ (27.08 и 10.09).

### Состав пакета — что уточнено
- **Размещение в поиске входит** в «Старт Медиа».
- **Логотип на карте — отдельная платная опция**, в пакет не входит.
  Цена: **36 000 ₸ за полгода** (6 000 ₸/мес, +19 % к пакету).
  Решение владельца — **берём**. Итого пакет + логотип = 224 268 ₸ / 6 мес
  (37 378 ₸/мес). Порог окупаемости: валовая с заказа ≈ 13 500 ₸ (фудкост
  ~7 %), значит логотип отбивают **2,7 дополнительных заказа за полгода** —
  один заказ раз в 2,2 месяца.
- Открыто: сколько рубрик входит, есть ли ПК-версия приложения, можно ли
  задать отдельный URL объявлению в выдаче.

### Что НЕ поднимать с менеджером (решение владельца, 30.08.2026)
Расхождение юрлиц между бланком заказа (ТОО «Городской сервис») и счётом
(ТОО «2ГИС – Справочник Караганды») и пустые пункты 5–6 (электронная почта)
**владелец закрывает сам**. В документы для 2ГИС не выносить.

### Базовая линия ДО пакета (01.06–30.08.2026)
| Куда | Сессий | Вовлечённых | Ср. длит. |
|---|---|---|---|
| vkusbuket.kz (`link.2gis.ru / referral`) | 4 | 3 | 50 с |
| klubnikavshokolade.kz (`link.2gis.com / referral`) | 1 | 1 | 353 с |

**5 сессий за 3 месяца = 1,7 сессии/мес.** Это ноль, от которого считать эффект.
Вовлечённость 4 из 5 (80 %) — выше, чем у `google / cpc` (36–42 %). Трафик
тёплый, но его почти нет.

### Ключевое: из 2ГИС не ходят на сайт — оттуда звонят и пишут
Мерить пакет кликами по сайту нельзя. Бизнес живёт на диалогах в WhatsApp
(чемпион Meta даёт их по $1,27). Значит и 2ГИС мерить диалогами.

### Ссылки, которые ставить в карточку
Метки в существующей схеме (`ig/paid`, `fb/paid` → `2gis/paid`):

- Поле «сайт» в карточке:
  `https://vkusbuket.kz/katalog?utm_source=2gis&utm_medium=paid&utm_campaign=start_media&utm_content=card`
- Объявление в поисковой выдаче (отделяет платное от бесплатной карточки):
  `…&utm_content=search_ad`
- **WhatsApp — главный измеритель:**
  `https://wa.me/77777667663?text=Здравствуйте!%20Пишу%20из%202ГИС`
  Предзаполненный текст приходит в чат = атрибуция без аналитики.

Вести на `/katalog`, а не на `/`: рубрика товарная, человек ищет товар.

⚠️ Проверить через 2–3 дня после запуска, что метки переживают редирект
`link.2gis.ru`. Искать в GA4 источник `2gis / paid`. Если остался
`link.2gis.ru / referral` — метки срезаются, остаётся только WhatsApp-ссылка.

### Что запросить у менеджера
- Письменный состав пакета «Старт Медиа» (в бланке перечислен один элемент).
- Сколько рубрик входит; добавить «Доставка цветов», «Кондитерские изделия».
- Входит ли ПК-версия приложения (в бланке только мобильные + 2gis.kz).
- Можно ли задать отдельный URL для объявления в выдаче.
- Отчётность у менеджера **не запрашивать** — она есть в кабинете 2ГИС.

## Открытые вопросы

- Кабинет `8747974784`: подключение к Windsor закрывает **ровно один вопрос —
  сколько стоит Google**. Конверсии (884) GA4 и так отдаёт; расход — нет.
  Если расход известен из интерфейса Google Ads, подключение не даёт ничего
  нового. Не поднимать как «критично».
- Погасить задолженность на `1686952041405037` и `586810819730079` —
  UNSETTLED на бизнес-аккаунте рискует блокировкой рабочего кабинета.
- Найти кабинет, которому принадлежит кампания `…0226`.
- Добавить поле источника в карточку клиента POS — это единственное, что
  замыкает петлю «реклама → продажа в кассе».
- Почистить два тестовых заказа в GA4.
