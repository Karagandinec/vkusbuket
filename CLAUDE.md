# Вкус Букет (Караганда) — карта экосистемы

> Индекс проекта. Проверено по кабинетам 30–31.08.2026.
> **Читать до любых выводов про рекламу, аналитику и атрибуцию.**
> Рекламная петля уже построена и работает. Не заявлять, что «аналитики нет»
> или «UTM не настроены», не сверившись с этим файлом и с кабинетами.

## ⛔ Главное: `vkusbuket.kz` — ЧУЖОЙ САЙТ

`vkusbuket.kz` — **астанинский бизнес, к нам отношения не имеет.**
Доступ к его данным есть, но это не наши цифры. Вместе с ним НЕ наши:

| Не наше | ID | Что это |
|---|---|---|
| GA4 «vkusbuket.kz - Караганда» | `397943910` | название вводит в заблуждение — это Астана |
| Google Ads «vkusbuket.kz_new» | `8747974784` | Performance Max астанинского сайта |

⚠️ Не сводить эти данные с нашими, не считать их трафик нашим, не поднимать
вопрос о подключении кабинета `8747974784` — это не наш расход.
Страница `vkusbuket.kz/katalog` подписана «Каталог Астана» — не ссылаться.

## Наша экосистема

**Сайт один: `klubnikavshokolade.kz`** (Tilda). Всё остальное крутится на него.

| Слой | Что именно | ID |
|---|---|---|
| Сайт | `klubnikavshokolade.kz` | GA4 `548805252` |
| Поиск | Search Console | `klubnikavshokolade.kz` |
| Google Ads | одна Search-кампания «Клубника в шоколаде» | `3977282678` (397-728-2678) |
| Meta Ads | «Вкус Букет · AI Караганда» | `3522529827982603` |
| Instagram | `@vkusbuket.karaganda` | `17841410537730847` |
| POS / бэк-офис | этот репозиторий + Supabase | — |

Страницы сайта (по посещаемости): `/katalog/` — самая ходовая, `/`,
`/povod/1s/` (1 сентября), `/otzyv/`, `/o-nas/`, `/course/`, `/kontakty/`.

Этот репозиторий — **только POS/бэк-офис** (React + Supabase): касса, склад,
производство, смены, предзаказы, отчёты. Сайта и рекламного кода здесь нет и
быть не должно. Аналитику искать в кабинетах, а не в `src/`.

### Прочие кабинеты Meta (Business `238339980390445`)
| Кабинет | ID | Статус |
|---|---|---|
| Вкус Букет · AI Караганда | `3522529827982603` | **ACTIVE — рабочий** |
| Виталий Бош | `465521244732733` | ACTIVE, расхода нет |
| — | `1686952041405037` | **UNSETTLED** |
| Казахстан Новый | `586810819730079` | **UNSETTLED** |
| SMM_PRO | `2045584352547123` | CLOSED |

Суффикс ID кампании = кабинет: `…0455` → AI Караганда, `…0715` → Виталий Бош.

## Рекламная петля — как размечено (работает)

Метки живут в `session_source/medium/campaign/content` GA4
(в `landing_page` их не искать — GA4 их оттуда вырезает).

| Точка входа | source / medium | campaign | content |
|---|---|---|---|
| Meta, лента Instagram | `ig / paid` | ID кампании Meta | ID объявления |
| Meta, Facebook | `fb / paid` | ID кампании | ID объявления |
| Meta, Audience Network | `an / paid` | ID кампании | ID объявления |
| Meta, Threads | `th / paid` | ID кампании | ID объявления |
| Ссылка в шапке Instagram | `ig / social` | — | `link_in_bio` |
| Google Ads | `google / cpc` | имя кампании | автопометка |
| ChatGPT | `chatgpt.com / ai-assistant` | — | — |
| **2ГИС** | `link.2gis.com` referral | — | — |

⚠️ GA4 относит `ig/fb/an/th` не в «Paid Social», а в «Paid Other» и
«Cross-network» — его дефолтный список источников их не знает. Это дефект
группировки GA4, **не** отсутствие меток. Смотреть source/medium, а не
Default channel group.

## Как устроена реклама Meta: чемпион-претендент

Схема «чемпион–претендент», а не «крутим всё подряд». Читается в именах:
`Ступень 1` / `Купившие` / `Горячие` — стадия воронки; `чемпионы`,
`чемпион-сборка` — победившая связка; `v2` — итерация; `пост-1` vs
`ролики 9:16` — тест креатива; `все плейсменты` vs `stories+reels` vs
`авто-плейсменты` — тест размещений; `муж 30–60` vs `жен 25–65` vs `18–65` —
тест аудиторий; `подарок-2` vs `чайная пара` — тест оффера.
`1С` в именах = **1 сентября** (посадочная `/povod/1s/`).
География почти везде `Караганда 30км`.

**Последняя правка кабинета — 22.08.2026 17:58.** Активна ровно одна кампания:
`WA Переписки · 1С v2 · чемпион-сборка · 16.08`, бюджет $10/день,
цель OUTCOME_ENGAGEMENT. Остальное **выключено намеренно** — консолидация
в чемпиона, а не простой.

Цена диалога: `Ступень 1` (июль) $1,99 → 11–20.08 $1,98 → **21–30.08 $1,27**.
За те же деньги диалогов стало в полтора раза больше. Схема улучшается.

⚠️ **Не называть паузы «простоем».** Дни без показов = выключенные претенденты.
Прежде чем говорить о простое — смотреть `effective_status` и `updated_time`.

## Качество трафика (16–29.08, klubnikavshokolade.kz)

| Источник | Сессий | Вовлечён. | Ср. длит. |
|---|---|---|---|
| `google / cpc` | 279 | 36 % | 58 с |
| `google / organic` | 45 | 51 % | 194 с |
| `ig / social` | 29 | **97 %** | 144 с |
| `l.instagram.com` | 2 | 100 % | **675 с** |
| `chatgpt.com` | 7 | 86 % | 376 с |

Платный трафик сидит на сайте втрое меньше и вовлекается вдвое хуже органики
и Instagram. **Отсутствие продаж по рекламным кликам — свойство трафика, а не
поломка учёта.**

⚠️ **Запрет на вывод «петля разорвана».** Ноль продаж по рекламному клику
означает ровно одно: пришедшие по рекламе не купили. Прежде чем говорить
о разрыве — показать, что событие не доехало, а не что продажи нет.

## Учёт денег

- **Электронная торговля GA4 настроена верно.** 7 из 11 транзакций совпали
  с `products.json` до тенге (Набор 25 = 18 900, Набор 8 = 7 500,
  Букет XS = 13 400, Набор 12 = 10 200, Букет S = 15 900, пара 8 шт + XS =
  20 900). Мусор: два тестовых заказа на `1 ₸` и `100 ₸`.
  Реальная выручка 06–29.08 — **111 340 ₸ за 9 заказов**, средний чек
  **12 371 ₸**.
- **Валовая прибыль 93 %** (фудкост ~7 %) → **≈13 500 ₸ с заказа**.
- **Продажи приходят серверно**: транзакции летят с `(not set)` источником
  и без посадочной. Это отправка с бэкенда, а не потеря данных.
- **GA4 ↔ POS не связаны.** В карточке клиента (`POS.js:124`) поля источника
  нет: `id, name, phone, discount_percent, created_at`. Все `source` в коде —
  способ оплаты (`Expenses.js`) или локация склада (`Production.js`).

## 2ГИС — статус сделки (31.08.2026)

**Бланк заказа `БЗ_84-84-935800` от 27.08.2026, НЕ подписан.**
Пакет «Старт Медиа»: объявление в поисковой выдаче, рубрика «Сувенирные
композиции». 188 268 ₸ за 6 мес (31 378 ₸/мес), выход 01.09.2026–28.02.2027,
два платежа по 94 134 ₸.

**Логотип на карте** — отдельная опция, **36 000 ₸ за полгода** (6 000 ₸/мес,
+19 % к пакету). Решение: **берём.** Итого 224 268 ₸ / 6 мес.
Окупаемость: при валовой 13 500 ₸ с заказа логотип отбивают **2,7 заказа за
полгода** — один раз в 2,2 месяца.

**Размещение в поиске входит** в пакет.

### Базовая линия ДО пакета
2ГИС даёт `klubnikavshokolade.kz` **1 сессию за 3 месяца** (`link.2gis.com`,
353 секунды на сайте). Это ноль, от которого считать эффект.

### Ключевое: из 2ГИС не ходят на сайт — оттуда звонят и пишут
Мерить пакет кликами нельзя. Бизнес живёт на диалогах в WhatsApp. Значит
и 2ГИС мерить диалогами.

### Ссылки для карточки (проверено, все ASCII)
- Поле «сайт» и объявление:
  `https://klubnikavshokolade.kz/katalog/?utm_source=2gis&utm_medium=paid&utm_campaign=start_media&utm_content=card`
  (для объявления в выдаче — `utm_content=search_ad`)
- **WhatsApp, главный измеритель:**
  `https://wa.me/77777667663?text=%D0%97%D0%B4%D1%80%D0%B0%D0%B2%D1%81%D1%82%D0%B2%D1%83%D0%B9%D1%82%D0%B5%21%20%D0%9F%D0%B8%D1%88%D1%83%20%D0%B8%D0%B7%202%D0%93%D0%98%D0%A1`
  Раскодируется в «Здравствуйте! Пишу из 2ГИС». **Кириллицу в URL не писать** —
  ломается в формах.

⚠️ Через 2–3 дня после запуска проверить, что метки переживают редирект
`link.2gis.com`: искать в GA4 источник `2gis / paid`.

### Что запросить у менеджера
- Письменный состав пакета «Старт Медиа» (в бланке перечислен один элемент).
- Сколько рубрик входит; добавить «Доставка цветов», «Кондитерские изделия».
- Входит ли ПК-версия приложения.
- Можно ли задать отдельный URL объявлению в выдаче.
- **Отчётность не запрашивать** — она есть в кабинете 2ГИС.

### Что НЕ поднимать (решение владельца)
Расхождение юрлиц между бланком заказа (ТОО «Городской сервис») и счётом
(ТОО «2ГИС – Справочник Караганды») и пустые пункты 5–6 (почта) — владелец
закрывает сам. В документы для 2ГИС не выносить.

## Как правильно смотреть цифры

- **Meta:** MCP `mcp__Meta_ads__*`, кабинет `3522529827982603`.
- **Google Ads / GA4 / Search Console / Instagram:** Windsor.ai
  (`get_fields` → `get_data`). Supermetrics не авторизован ни к чему.
- **GA4:** брать аккаунт `548805252`. Аккаунт `397943910` — чужой.
- **Каналы:** `session_source_medium`, а не `session_default_channel_group`.
- **Свежесть:** смотреть последние 7–14 дней, а не месячную давность.
- **Валюта:** расходы в USD, выручка в тенге. Курс не подставлять.
- **Ссылки:** прежде чем давать URL — проверять, куда он ведёт и как
  называется страница.

## Открытые вопросы

- Google Ads `3977282678`: **26.08 показы упали в 50 раз** (1 855 → 39) и не
  восстановились. Кампания крутится, но остался узкий брендовый срез.
  Разобраться, что изменилось.
- Погасить задолженность на `1686952041405037` и `586810819730079` —
  UNSETTLED рискует блокировкой рабочего кабинета.
- Найти кабинет Meta, которому принадлежит кампания с суффиксом `…0226`.
- Добавить поле источника в карточку клиента POS — единственное, что замыкает
  петлю «реклама → продажа в кассе».
- Почистить два тестовых заказа в GA4.
