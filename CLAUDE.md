# Вкус Букет · Караганда — карта экосистемы

> Единая карта. Собрана 31.08.2026 проверкой всех кабинетов, не по памяти.
> **Читать целиком до любых выводов про рекламу, аналитику, атрибуцию и сайты.**

---

## ⛔ ЧТО НЕ НАШЕ — проверить первым делом

`vkusbuket.kz` — **астанинский бизнес. К нам не относится.**
Доступ к его данным в кабинетах есть, но это чужие цифры.

| НЕ НАШЕ | ID | Почему путает |
|---|---|---|
| Сайт `vkusbuket.kz` | — | страница `/katalog` подписана «Каталог Астана» |
| GA4 «vkusbuket.kz - Караганда» | `397943910` | в названии стоит «Караганда», но это Астана |
| Google Ads «vkusbuket.kz_new» | `8747974784` | Performance Max: `S-Performance`, `LPM - 11`, `14.02.2026`, `Создание спроса` |
| Кабинет Meta с суффиксом кампаний `…0226` | не в доступе | кампания `120253721507010226` |

**Разметка `ig / paid`, `fb / paid`, `an / paid`, `th / paid` — тоже астанинская.**
Проверено: за 01.06–31.08 все 321 сессия с этими метками пришли на
`vkusbuket.kz`, все от одной кампании `120253721507010226` и одного объявления
`120253721508580226`. **На `klubnikavshokolade.kz` таких сессий ноль.**

⚠️ Никогда: не сводить эти данные с нашими, не называть их трафик нашим,
не поднимать подключение кабинета `8747974784` — это не наш расход.

---

## ✅ ЧТО НАШЕ

Бренд — **«Вкус Букет»**, город — **Караганда**, сайт — **`klubnikavshokolade.kz`**.
Название бренда и домен сайта не совпадают. Это нормально, так и есть.

| Слой | Что именно | ID / адрес |
|---|---|---|
| **Сайт** | `klubnikavshokolade.kz` (Tilda) | GA4 `548805252` |
| **Поиск** | Search Console | `https://klubnikavshokolade.kz/` |
| **Google Ads** | Search «search, http // Клубника в шоколаде» | `3977282678` (397-728-2678) |
| **Meta Ads** | «Вкус Букет · AI Караганда» | `3522529827982603` |
| **Instagram** | `@vkusbuket.karaganda` | `17841410537730847` |
| **WhatsApp** | основной канал заказов | `+7 777 766 7663` |
| **2ГИС** | карточка + пакет «Старт Медиа» (не подписан) | бланк `БЗ_84-84-935800` |
| **POS / бэк-офис** | этот репозиторий + Supabase | касса, склад, производство |

Страницы сайта по посещаемости: `/katalog/` (самая ходовая), `/`,
`/povod/1s/` (1 сентября), `/otzyv/`, `/o-nas/`, `/course/`, `/kontakty/`,
`/katalog/schgold/`.

Прочие кабинеты Meta в бизнес-аккаунте `238339980390445` «Vkusbuket.karaganda»:
`465521244732733` «Виталий Бош» (ACTIVE, расхода нет),
`1686952041405037` (**UNSETTLED**), `586810819730079` «Казахстан Новый»
(**UNSETTLED**), `2045584352547123` SMM_PRO (CLOSED).
Суффикс ID кампании: `…0455` → AI Караганда, `…0715` → Виталий Бош.

---

## КАК ЗАКОЛЬЦОВАНО: две отдельные петли

Ключ ко всему: **у нас две петли, и они не пересекаются.**

### Петля 1. Meta → WhatsApp → касса (основные деньги)

```
Meta Ads (3522529827982603)
  цель OUTCOME_ENGAGEMENT
        ↓  объявление ведёт СРАЗУ в WhatsApp, минуя сайт
WhatsApp диалог  ← результат кампании: messaging_conversation_started
        ↓  менеджер согласовывает заказ в переписке
Заказ → POS (Supabase) → выручка, COGS, смена
```

**Сайта в этой петле нет.** Поэтому в GA4 не видно ни одного `ig/paid` на
`klubnikavshokolade.kz` — и это не поломка, а конструкция.
Отдача меряется ценой диалога в кабинете Meta, а не сессиями в GA4.

### Петля 2. Поиск и профиль → сайт → заказ (онлайн-продажи)

```
Google Ads (3977282678) ─ google / cpc ──┐
Google органика ─────── google / organic ┤
Instagram, ссылка в шапке ─ ig / social  ├→ klubnikavshokolade.kz
ChatGPT ──────── chatgpt.com/ai-assistant┤        ↓
2ГИС ─────── link.2gis.com / referral ───┘   оформление заказа
                                                  ↓
                              бэкенд шлёт покупку в GA4 серверно
                              (source = (not set), посадочной нет)
                                                  ↓
                                        выручка в GA4 + POS
```

### Петля ЗАМКНУТА серверно — в Supabase есть слой атрибуции

⚠️ **Не искать поле «источник» в React-коде POS — его там нет и не должно быть.**
Связка «реклама → продажа в кассе» сделана в базе, по телефону.
Проект Supabase `zsplctffdvyzbzlnxuew` (`sweetsync-saas`), таблицы:

| Таблица | Что делает |
|---|---|
| `site_clicks` | клики «написать в WhatsApp / позвонить» с сайта + `gclid`, `fbc`, `fbp`, `ym_uid`, `ga_client_id`, все `utm_*` |
| `site_attribution` | привязка телефона к визиту по коду из первого сообщения WhatsApp |
| `ctwa_attribution` | Click-to-WhatsApp из Meta: `ctwa_clid`, `source_id`, `headline` |
| `wa_messages` | переписка WhatsApp (9 688 строк) |
| `phone_match_log` | автосверка касса ↔ WhatsApp: кандидаты, score, причины |
| `conversion_log` | что и куда отправлено по каждой оплаченной продаже: Meta, GA4/Google Ads, Яндекс |
| `ad_stats_daily` | расход/диалоги по кампаниям |
| `crm_profiles` / `crm_orders` / `crm_events` | CRM: 8 570 профилей, 10 964 заказа |

**Как работает:** клик по кнопке на сайте пишется в `site_clicks` с рекламными
ID → человек пишет в WhatsApp с кодом → `site_attribution` связывает телефон
с визитом → продажа в кассе матчится по телефону (`phone_match_log`) →
`conversion_log` отправляет покупку обратно в Meta, GA4 и Яндекс.

### Реальное покрытие (09–31.08.2026, 136 продаж в `conversion_log`)

| Канал | Отправлено | Пропущено | Причина пропуска |
|---|---|---|---|
| GA4 | 10 | 126 | `no site visit` |
| Meta (сайт) | 10 | 126 | `no site visit` |
| Яндекс | 9 | 127 | `no site visit` |
| Meta CTWA | 5 | 132 | `no ctwa_clid` |

**Механика работает, покрытие ~7 %.** Причина одна: 126 из 136 покупателей
не проходили через размеченную точку входа — пришли прямо в WhatsApp,
по телефону, повторно или самовывозом. Это структура спроса, а не поломка.

`site_clicks`: 175 кликов, рекламный ID есть у 173 (gclid 103, fbc 42,
ym_uid 28). По источникам: без utm 117 + 13 звонков, `ig` 30 + 1 звонок,
`chatgpt.com` 10 + 3 звонка, **`2gis` — 1**.

⚠️ **Запрет на вывод «петля разорвана».** Она замкнута. Низкое покрытие
означает, что покупатели заходят мимо размеченных точек, а не что учёт сломан.
Каждая новая размеченная точка входа (карточка 2ГИС с WhatsApp-ссылкой)
поднимает покрытие.

---

## ФАКТИЧЕСКИЕ ЦИФРЫ (проверены 31.08.2026)

### Сайт `klubnikavshokolade.kz`, источники за 01.06–31.08
| Источник | Сессий | Ключевых событий |
|---|---|---|
| `google / cpc` | 450 | 103 |
| `(direct) / (none)` | 145 | 7 |
| `google / organic` | 102 | 19 |
| `ig / social` (content `link_in_bio`) | 70 | 27 |
| `chatgpt.com / ai-assistant` | 26 | 5 |
| `facebook.com / referral` | 10 | 0 |
| `l.instagram.com / referral` | 7 | 5 |
| `yandex.kz`, `youtube.com`, `weather.com`, `ksint.kaspi.kz` | 10 | 2 |
| `link.2gis.com / referral` | **1** | 0 |
| `(not set)` — серверные заказы | 3 | **11 заказов, 111 441 ₸** |

### Качество трафика (16–29.08)
| Источник | Вовлечённость | Ср. длительность |
|---|---|---|
| `google / cpc` | 36 % | 58 с |
| `google / organic` | 51 % | 194 с |
| `ig / social` | **97 %** | 144 с |
| `chatgpt.com` | 86 % | 376 с |

Платный сидит втрое меньше и вовлекается вдвое хуже органики и Instagram.
**Это свойство трафика, а не поломка учёта.**

### Meta (кабинет `3522529827982603`)
Схема **чемпион–претендент**. Имена кодируют тест: `Ступень 1` / `Купившие` /
`Горячие` — стадия воронки; `чемпионы`, `чемпион-сборка` — победившая связка;
`v2` — итерация; `пост-1` vs `ролики 9:16` — креатив; `все плейсменты` vs
`stories+reels` vs `авто-плейсменты` — размещения; `муж 30–60` vs `жен 25–65`
vs `18–65` — аудитории; `подарок-2` vs `чайная пара` — оффер.
**`1С` = 1 сентября** (посадочная `/povod/1s/`). География `Караганда 30км`.

Последняя правка кабинета — **22.08.2026 17:58**. Активна ровно одна кампания:
`WA Переписки · 1С v2 · чемпион-сборка · 16.08`, $10/день.
Остальное выключено **намеренно** — консолидация в чемпиона.

| Период | Расход | Диалогов | Цена диалога |
|---|---|---|---|
| Июль (`Ступень 1`) | $438 | 220 | $1,99 |
| 11–20.08 | $92,86 | 47 | $1,98 |
| **21–30.08** | $94,32 | **74** | **$1,27** |

⚠️ **Не называть паузы «простоем».** Дни без показов = выключенные
претенденты. Смотреть `effective_status` и `updated_time`.

### Google Ads `3977282678`
Август: 22 462 показа, 505 кликов, $70,00, CPC $0,14, CTR 2,25 %.
🔴 **26.08 показы упали в 50 раз** (1 855 → 39) и не восстановились.
Кампания крутится, но остался узкий брендовый срез. Не разобрано.

### Instagram `@vkusbuket.karaganda`
Август: охват **75 037**, в среднем 2 587/день.

### Search Console
Август: 62 клика, 2 550 показов, средняя позиция 11,7.

### Деньги
- Электронная торговля GA4 **настроена верно**: 7 из 11 транзакций совпали
  с `products.json` до тенге (Набор 25 = 18 900, Набор 8 = 7 500,
  Букет XS = 13 400, Набор 12 = 10 200, Букет S = 15 900, пара 8 шт + XS = 20 900).
- Мусор: два тестовых заказа на `1 ₸` и `100 ₸`. Реальная выручка 06–29.08 —
  **111 340 ₸ за 9 заказов**, средний чек **12 371 ₸**.
- Валовая прибыль **93 %** (фудкост ~7 %) → **≈13 500 ₸ с заказа**.

---

## 2ГИС — статус (31.08.2026)

Бланк заказа `БЗ_84-84-935800` от 27.08.2026, **не подписан**.
188 268 ₸ / 6 мес = **31 379 ₸/мес**, выход 01.09.2026–28.02.2027.
Рубрика в бланке — «Сувенирные композиции».

### Состав пакета «Старт Медиа» (со слов менеджера, 31.08.2026)
- **Позиция в топ-списке 5–7** в поисковой выдаче справочника
- **Приоритет можно докупить** — позиция выше 5–7 за отдельные деньги
- **Брендирование** карточки
- **Логотип**
- **Спецпредложение** в карточке
- **Кнопка действия** (CTA) ← сюда ставить WhatsApp-ссылку
- **Витрина** товаров ← сюда ставить ссылки на каталог
- **Обжалование отзыва** в течение недели
- **Скрытие фото**
- **Показ в блоке «Похожие организации»** у конкурентов без рекламы

### Ответы менеджера (голосовое, 31.08.2026)
- **ПК-версия НЕ входит** — «у нас она отключена». Только мобильные версии
  справочника и онлайн-версия 2gis.kz. Вопрос закрыт.
- **Рубрики: добавлять невыгодно.** Если добавить «Кондитерские изделия»,
  «Доставку цветов» и «Подарки» — прайс меняется кардинально, **минимум
  в два раза дороже**. То есть 188 268 ₸ → 376 000 ₸+. Не берём.
- **Ключевая фраза «Клубника в шоколаде» заведена в рубрику «Сувенирные
  композиции»** — то есть по профильному запросу карточку находят и без
  дополнительных рубрик. Это снимает исходное возражение к рубрике.

⚠️ **Противоречие, требует уточнения:** логотип назван в составе пакета, но
отдельно озвучена цена **36 000 ₸ / 6 мес за логотип на карте**. Скорее всего
это разные вещи: логотип в карточке/выдаче входит, логотип-пин на карте —
платный. Подтвердить у менеджера письменно, прежде чем платить.

Решение по логотипу на карте: **берём** (если это действительно отдельная
опция). Итого 224 268 ₸. Окупаемость: 2,7 заказа за полгода = раз в 2,2 месяца.

**База до пакета: 1 сессия за 3 месяца** (`link.2gis.com`, 353 секунды).

**Из 2ГИС не ходят на сайт — оттуда звонят и пишут.** Мерить диалогами.

### Доставка (решение владельца — НЕ пересчитывать)
- **Бесплатная доставка от 50 000 ₸.**
- Ниже 50 000 ₸ — платная, **по категориям, которые уже заполнялись ранее**.
- Зона доставки для 2ГИС сдаётся файлом **KML** — это требование площадки.
  Зоны у владельца уже готовы; радиусы от себя не придумывать.

### Ссылки для карточки (проверены, все ASCII)
- Сайт и объявление:
  `https://klubnikavshokolade.kz/katalog/?utm_source=2gis&utm_medium=paid&utm_campaign=start_media&utm_content=card`
  (для объявления в выдаче — `utm_content=search_ad`)
- **WhatsApp, главный измеритель:**
  `https://wa.me/77777667663?text=%D0%97%D0%B4%D1%80%D0%B0%D0%B2%D1%81%D1%82%D0%B2%D1%83%D0%B9%D1%82%D0%B5%21%20%D0%9F%D0%B8%D1%88%D1%83%20%D0%B8%D0%B7%202%D0%93%D0%98%D0%A1`
  = «Здравствуйте! Пишу из 2ГИС». **Кириллицу в URL не писать** — ломается.

Осталось запросить: логотип входит или 36 000 ₸ отдельно, и цена приоритета
выше позиции 5–7. Рубрики и ПК-версия — закрыты (см. ответы менеджера).
**Отчётность не запрашивать** — она в кабинете 2ГИС.
**Юрлица и пустые поля бланка не поднимать** — владелец закрывает сам.

---

## ПРАВИЛА РАБОТЫ

1. **GA4 брать только `548805252`.** `397943910` — чужой.
2. **Google Ads брать только `3977282678`.** `8747974784` — чужой.
3. **Meta:** MCP `mcp__Meta_ads__*`, кабинет `3522529827982603`.
   Отвечают на запросы только он и `465521244732733`.
4. **Каналы:** `session_source_medium`, не `session_default_channel_group` —
   GA4 кладёт `ig/fb/an/th` в «Paid Other» и «Cross-network».
5. **Метки:** искать в `session_source/medium/campaign/content`,
   в `landing_page` их нет — GA4 вырезает.
6. **Свежесть:** окно 7–14 дней. Месячная давность — уже история.
7. **Ссылки:** прежде чем давать URL — проверить, куда ведёт и как называется
   страница.
8. **Windsor.ai** для GA4 / Google Ads / Search Console / Instagram
   (`get_fields` → `get_data`). Supermetrics не авторизован ни к чему.
9. **Валюта:** расход в USD, выручка в тенге. Курс не подставлять.
10. **Репозиторий** — только POS/бэк-офис. Сайта и рекламного кода здесь нет.

## Открытые вопросы

- Google Ads `3977282678`: почему 26.08 показы упали в 50 раз.
- Погасить UNSETTLED на `1686952041405037` и `586810819730079` — риск
  блокировки рабочего кабинета.
- Поднять покрытие атрибуции: доезжает 10 продаж из 136. Каждая новая
  размеченная точка входа добавляет покрытие.
- Почистить два тестовых заказа в GA4.
